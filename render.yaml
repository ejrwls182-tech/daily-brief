services:
  - type: web
    name: daily-brief
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: UPDATE_SECRET
        generateValue: true
